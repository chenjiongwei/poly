--25数据库
--查询是否开启CT
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_FeeCollectResult')),0))=0 ) BEGIN  SELECT  'x_FeeCollectResult'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_PreOrder')),0))=0 ) BEGIN  SELECT  's_PreOrder'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_OrderPrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_OrderPrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mdm_GCBuild')),0))=0 ) BEGIN  SELECT  'mdm_GCBuild'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Contract')),0))=0 ) BEGIN  SELECT  's_Contract'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_PrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_PrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_MortgageServiceFkdCT')),0))=0 ) BEGIN  SELECT  's_MortgageServiceFkdCT'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Invoice')),0))=0 ) BEGIN  SELECT  's_Invoice'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_YearJLPlanRhtz')),0))=0 ) BEGIN  SELECT  's_YearJLPlanRhtz'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Documents')),0))=0 ) BEGIN  SELECT  'p_Documents'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myWorkflowProcessEntity')),0))=0 ) BEGIN  SELECT  'myWorkflowProcessEntity'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_trade2cst')),0))=0 ) BEGIN  SELECT  's_trade2cst'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_PSBCScreenageBank')),0))=0 ) BEGIN  SELECT  's_PSBCScreenageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_InvoiceQzInfo')),0))=0 ) BEGIN  SELECT  'p_InvoiceQzInfo'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_UpComingSetting')),0))=0 ) BEGIN  SELECT  's_UpComingSetting'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_trade2cst')),0))=0 ) BEGIN  SELECT  's_trade2cst'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_room')),0))=0 ) BEGIN  SELECT  'p_room'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_BOCMortgageBank')),0))=0 ) BEGIN  SELECT  's_BOCMortgageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_ContractCollectResult')),0))=0 ) BEGIN  SELECT  'x_ContractCollectResult'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_DevelopmentCompany')),0))=0 ) BEGIN  SELECT  'p_DevelopmentCompany'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mdm_Project')),0))=0 ) BEGIN  SELECT  'mdm_Project'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mdm_LbProject')),0))=0 ) BEGIN  SELECT  'mdm_LbProject'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mid_s_InvoiceKps')),0))=0 ) BEGIN  SELECT  'mid_s_InvoiceKps'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_MipPayFeeBillStatusData')),0))=0 ) BEGIN  SELECT  's_MipPayFeeBillStatusData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_BookingCollectResult')),0))=0 ) BEGIN  SELECT  'x_BookingCollectResult'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Order')),0))=0 ) BEGIN  SELECT  's_Order'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_CNCBMortgageBank')),0))=0 ) BEGIN  SELECT  's_CNCBMortgageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_ABCMortgageBank')),0))=0 ) BEGIN  SELECT  's_ABCMortgageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Booking')),0))=0 ) BEGIN  SELECT  's_Booking'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Building')),0))=0 ) BEGIN  SELECT  'p_Building'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_FeePrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_FeePrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_BookingPrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_BookingPrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_CMBMortgageBank')),0))=0 ) BEGIN  SELECT  's_CMBMortgageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_PSBCMortgageBank')),0))=0 ) BEGIN  SELECT  's_PSBCMortgageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_NormStationSet')),0))=0 ) BEGIN  SELECT  's_NormStationSet'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('k_Pay')),0))=0 ) BEGIN  SELECT  'k_Pay'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mdm_Project')),0))=0 ) BEGIN  SELECT  'mdm_Project'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_CNCBScreenageBank')),0))=0 ) BEGIN  SELECT  's_CNCBScreenageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_GetinPrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_GetinPrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Fee')),0))=0 ) BEGIN  SELECT  's_Fee'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_MortgageServiceToYhjCT')),0))=0 ) BEGIN  SELECT  's_MortgageServiceToYhjCT'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_Getin')),0))=0 ) BEGIN  SELECT  's_Getin'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_TradeFlies')),0))=0 ) BEGIN  SELECT  's_TradeFlies'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_productroomarea')),0))=0 ) BEGIN  SELECT  's_productroomarea'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_BOCScreenageBank')),0))=0 ) BEGIN  SELECT  's_BOCScreenageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_GetinCollectResult')),0))=0 ) BEGIN  SELECT  'x_GetinCollectResult'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_ABCScreenageBank')),0))=0 ) BEGIN  SELECT  's_ABCScreenageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_OrderCollectResult')),0))=0 ) BEGIN  SELECT  'x_OrderCollectResult'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_ContractPrimaryChangeData')),0))=0 ) BEGIN  SELECT  'x_ContractPrimaryChangeData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('s_CMBScreenageBank')),0))=0 ) BEGIN  SELECT  's_CMBScreenageBank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('mdm_SaleBuild')),0))=0 ) BEGIN  SELECT  'mdm_SaleBuild'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('x_CollectResult')),0))=0 ) BEGIN  SELECT  'x_CollectResult'+'CT未开启'  END; 
