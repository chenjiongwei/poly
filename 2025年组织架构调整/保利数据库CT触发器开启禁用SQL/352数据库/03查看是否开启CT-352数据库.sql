--352数据库
--查看是否存在CT
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_HTFKApplyWFNodeEntity')),0))=0 ) BEGIN  SELECT  'cb_HTFKApplyWFNodeEntity'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_TargetStage2Project')),0))=0 ) BEGIN  SELECT  'cb_TargetStage2Project'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('wy_Room')),0))=0 ) BEGIN  SELECT  'wy_Room'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_Contract')),0))=0 ) BEGIN  SELECT  'cb_Contract'+'CT未开启'  END; 
--IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myTaskWake_bakYB')),0))=0 ) BEGIN  SELECT  'myTaskWake_bakYB'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ContractWFNodeEntity')),0))=0 ) BEGIN  SELECT  'cb_ContractWFNodeEntity'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_TaskReport')),0))=0 ) BEGIN  SELECT  'jd_TaskReport'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ExecYgAlterAmount')),0))=0 ) BEGIN  SELECT  'cb_ExecYgAlterAmount'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_SKBankRequestLog')),0))=0 ) BEGIN  SELECT  'cb_SKBankRequestLog'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_NodeFinishCanSealApply')),0))=0 ) BEGIN  SELECT  'cb_NodeFinishCanSealApply'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myWorkflowProcessEntity')),0))=0 ) BEGIN  SELECT  'myWorkflowProcessEntity'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ContractCommissionBillingAttach')),0))=0 ) BEGIN  SELECT  'cb_ContractCommissionBillingAttach'+'CT未开启'  END; 
--IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myTaskWake_bakWB')),0))=0 ) BEGIN  SELECT  'myTaskWake_bakWB'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('fy_ProviderYhjCt')),0))=0 ) BEGIN  SELECT  'fy_ProviderYhjCt'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_DTCostRecollect')),0))=0 ) BEGIN  SELECT  'cb_DTCostRecollect'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_MipPayFeeBillStatusData')),0))=0 ) BEGIN  SELECT  'cb_MipPayFeeBillStatusData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderChgNotice')),0))=0 ) BEGIN  SELECT  'p_ProviderChgNotice'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('idm_CompanyOrganization')),0))=0 ) BEGIN  SELECT  'idm_CompanyOrganization'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderBusinessContactApplyArea')),0))=0 ) BEGIN  SELECT  'p_ProviderBusinessContactApplyArea'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2Service')),0))=0 ) BEGIN  SELECT  'p_Provider2Service'+'CT未开启'  END; 
--IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ProjHyb')),0))=0 ) BEGIN  SELECT  'cb_ProjHyb'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderEmployee')),0))=0 ) BEGIN  SELECT  'p_ProviderEmployee'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2ServiceFreeze')),0))=0 ) BEGIN  SELECT  'p_Provider2ServiceFreeze'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_BiddingBuilding')),0))=0 ) BEGIN  SELECT  'p_BiddingBuilding'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProductType')),0))=0 ) BEGIN  SELECT  'p_ProductType'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_HtType')),0))=0 ) BEGIN  SELECT  'cb_HtType'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('CompanyJoin')),0))=0 ) BEGIN  SELECT  'CompanyJoin'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2ServiceCompany')),0))=0 ) BEGIN  SELECT  'p_Provider2ServiceCompany'+'CT未开启'  END; 
--IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ProjHyb2Cost2Yt')),0))=0 ) BEGIN  SELECT  'cb_ProjHyb2Cost2Yt'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_KeyNode')),0))=0 ) BEGIN  SELECT  'jd_KeyNode'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cg_ProviderRelationship')),0))=0 ) BEGIN  SELECT  'cg_ProviderRelationship'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderBusinessContact')),0))=0 ) BEGIN  SELECT  'p_ProviderBusinessContact'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_ProjectPlanExecute')),0))=0 ) BEGIN  SELECT  'jd_ProjectPlanExecute'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_ProjectPlanTaskExecute')),0))=0 ) BEGIN  SELECT  'jd_ProjectPlanTaskExecute'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2Unit')),0))=0 ) BEGIN  SELECT  'p_Provider2Unit'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider')),0))=0 ) BEGIN  SELECT  'p_Provider'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_StopOrReturnWork')),0))=0 ) BEGIN  SELECT  'jd_StopOrReturnWork'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cg_ProductServiceProperty')),0))=0 ) BEGIN  SELECT  'cg_ProductServiceProperty'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2ServiceQualification')),0))=0 ) BEGIN  SELECT  'p_Provider2ServiceQualification'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_Budget_Executing')),0))=0 ) BEGIN  SELECT  'cb_Budget_Executing'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderGrade')),0))=0 ) BEGIN  SELECT  'p_ProviderGrade'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2Bank')),0))=0 ) BEGIN  SELECT  'p_Provider2Bank'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_Provider2Type')),0))=0 ) BEGIN  SELECT  'p_Provider2Type'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderSource')),0))=0 ) BEGIN  SELECT  'p_ProviderSource'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_ProviderType')),0))=0 ) BEGIN  SELECT  'p_ProviderType'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_ContractCommissionBillingRulePdf')),0))=0 ) BEGIN  SELECT  'cb_ContractCommissionBillingRulePdf'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_DesignAlterToTZXT')),0))=0 ) BEGIN  SELECT  'cb_DesignAlterToTZXT'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_DealPayData')),0))=0 ) BEGIN  SELECT  'cb_DealPayData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myWorkflowNodeEntity')),0))=0 ) BEGIN  SELECT  'myWorkflowNodeEntity'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cg_ZLBidInfo_dl')),0))=0 ) BEGIN  SELECT  'cg_ZLBidInfo_dl'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cg_TacticCgAgreementProduct_AttachmentOrders')),0))=0 ) BEGIN  SELECT  'cg_TacticCgAgreementProduct_AttachmentOrders'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('p_HkbBiddingBuilding2BuildingWork')),0))=0 ) BEGIN  SELECT  'p_HkbBiddingBuilding2BuildingWork'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('XdjxAnsy')),0))=0 ) BEGIN  SELECT  'XdjxAnsy'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_Budget2Yt')),0))=0 ) BEGIN  SELECT  'cb_Budget2Yt'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('fy_AgencyFeeSettleFile')),0))=0 ) BEGIN  SELECT  'fy_AgencyFeeSettleFile'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_PayUpdTrigger')),0))=0 ) BEGIN  SELECT  'cb_PayUpdTrigger'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_TargetCostRevise')),0))=0 ) BEGIN  SELECT  'cb_TargetCostRevise'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('jd_RelativeWork')),0))=0 ) BEGIN  SELECT  'jd_RelativeWork'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_SKBankRequestLog')),0))=0 ) BEGIN  SELECT  'cb_SKBankRequestLog'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('cb_RepairOutputValueHistoryData')),0))=0 ) BEGIN  SELECT  'cb_RepairOutputValueHistoryData'+'CT未开启'  END; 
IF((SELECT ISNULL( CHANGE_TRACKING_MIN_VALID_VERSION( OBJECT_ID('myTaskWake')),0))=0 ) BEGIN  SELECT  'myTaskWake'+'CT未开启'  END; 
